let menuStatus = 'login'; // "login"/registered
const PopupSettings = {
    position: {
        top: window.innerHeight / 2,
        left: window.innerWidth / 2,
    },
    time: 2500,
};

const LoginCredentials = {
    maxLoginLength: 16,
    minLoginLength: 4,
    maxPasswordLength: 255,
    minPasswordLength: 8,
};

const AnimationSettings = {
    delayTime: 500,
    durationTime: 500,
};